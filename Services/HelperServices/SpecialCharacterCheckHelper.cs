using System;
using System.Collections.Generic;
using System.Text;

namespace Services.HelperServices
{
    public class SpecialCharacterCheckHelper
    {
        public static string SpecialCharacterCheck(string stringValue)
        {
            if (stringValue != null)
            {
                /*stringValue = stringValue.Replace("|", "-");
                stringValue = stringValue.Replace("#", "-");
                stringValue = stringValue.Replace("$", "-");
                stringValue = stringValue.Replace("%", "-");
                stringValue = stringValue.Replace("@", "-");
                stringValue = stringValue.Replace("^", "-");
                stringValue = stringValue.Replace("`", "-");
                stringValue = stringValue.Replace("~", "-");
                stringValue = stringValue.Replace(">", "-");
                stringValue = stringValue.Replace("<", "-");
                stringValue = stringValue.Replace("_", "-");
                stringValue = stringValue.Replace(@"\", "-");

                stringValue = stringValue.Replace("[", "-");
                stringValue = stringValue.Replace("'", "-");
                stringValue = stringValue.Replace("(", "-");
                stringValue = stringValue.Replace(")", "-");
                stringValue = stringValue.Replace("*", "-");
                stringValue = stringValue.Replace("+", "-");
                stringValue = stringValue.Replace(",", "-");
                stringValue = stringValue.Replace("]", "-");
                //stringValue = stringValue.Replace(".", "-");
                stringValue = stringValue.Replace(":", "-");
                stringValue = stringValue.Replace("=", "-");
                stringValue = stringValue.Replace("!", "-");
                stringValue = stringValue.Replace("?", "-");
                stringValue = stringValue.Replace("&", "-");
                stringValue = stringValue.Replace("/", "-");
                stringValue = stringValue.Replace(";", "-");*/

            }

            return stringValue;

        }
    }
}




